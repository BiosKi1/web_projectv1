<nav class="navbar navbar-default navbar-fixed-top" style="background-color: white;box-shadow: 0 2px 4px -1px rgba(0,0,0,0.06),0 4px 5px 0 rgba(0,0,0,0.06),0 1px 10px 0 rgba(0,0,0,0.08) !important;">
    <div class="container">
        <div class="navbar-header">
            <a class="navbar-brand" href="{{ path('homepage') }}">Japan Day</a>
        </div>
        <div class="collapse navbar-collapse" id="navB">
            <ul class="nav navbar-nav navbar-right">
                <li><a href="{{ path('homepage') }}">Accueil</a></li>

                {% if is_granted('ROLE_ADMIN') %}
                    <li><a href="{{ path('users_index') }}">Liste des participants</a></li>
                    <li><a href="{{ path('event_night_contact') }}">Contact</a></li>
                {% else %}
                    <li><a href="{{ path('users_index') }}">Connexion</a></li>
                    <li><a href="{{ path('users_new') }}">Inscription</a></li>
                    <li><a href="{{ path('event_night_contact') }}">Contact</a></li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>